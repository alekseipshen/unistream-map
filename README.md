# ЮНИСТРИМ — обменные пункты у метро (Москва)

Внутренний справочник: 16 отделений АО КБ «ЮНИСТРИМ» на карте Москвы + все станции метро/МЦК/МЦД.
Выбор станции → ближайшие отделения, время пешком, адрес, «как найти», график, телефон и живые курсы валют.

- Данные отделений и курсы: официальный API unistream.ru (`/api/poses/exchange/{id}`)
- Курсы подтягиваются в браузере при открытии страницы и каждые 10 минут (CORS у API открыт)
- Станции метро: api.hh.ru/metro/1
- Карта: Leaflet + OpenStreetMap (всё self-hosted, кроме тайлов)

## Обновить статические данные (адреса, график, состав отделений)

```bash
python3 scripts/build_data.py --fetch   # тянет всё заново из API
git add -A && git commit -m "refresh data" && git push
```

Состав отделений: `scripts/branch-ids.json` (номер офиса → id в системе Юнистрим).
Найти id нового офиса: `https://api7.unistream.com/api/v1/poses/search?location=55.75,37.62&radius=25000&maxResults=200&filter.agent=11153`

## Конкуренты и их курсы

В карточке отделения — сводка «лучше всех покупают / дешевле всех продают» в радиусе 1 км;
красным подсвечено то, что выгоднее нашего. На карте: красная точка = обыгрывает нас, серая = нет.

Два источника курсов, разной точности:

- **Курс конкретного офиса** — banki.ru, эндпоинт `/products/currencyNodejsApi/getExchangesCoordinates/`
  (один запрос на валюту отдаёт все обменные пункты Москвы с координатами и курсом).
  Обязателен заголовок `X-Requested-With: XMLHttpRequest`, иначе 404. ~20 банков.
- **Курс банка по городу** — mainfin.ru (`scripts/remote_fetch_mainfin.py`), ~50 банков:
  ВТБ, Газпромбанк, РСХБ, Совкомбанк, Райффайзен, ПСБ, Русский Стандарт, Фора-Банк, Авангард,
  Уралсиб, Абсолют, Зенит, УБРиР, Экспобанк, Т-Банк и др. — их нет на banki.ru.
  Привязывается к точкам OSM по названию банка (`ALIASES` в `collect_bank_rates.py` — там,
  где написание расходится). В интерфейсе помечается «*» и подписью «курс по городу».
- ⚠️ **banki.ru и mainfin.ru отдают данные только на российский IP** (иначе anti-bot заглушка и 403),
  поэтому HTTP-запросы выполняются на RuVDS через ssh (`scripts/remote_fetch_*.py`),
  а обработка и git push — на Hetzner.
- Обновление: systemd timer `unistream-competitors.timer` на Hetzner, **каждые 30 минут
  с 08:05 до 22:35 МСК**. Сервис прогоняет оба сборщика с `--no-commit`, затем `publish.py`
  делает один коммит на все изменившиеся `data/*.js` (иначе GitHub Pages пересобирался
  дважды за прогон). Файлы, где поменялась только отметка времени, откатываются.
  При сбое пишет в топик Alert (7113).
- ⚠️ Раньше `offices_mainfin.js` писался, но не добавлялся в коммит — данные по отделениям
  mainfin висели замороженными. Поэтому публикация вынесена в один общий скрипт: добавляешь
  новый файл данных — он подхватится автоматически.
- Рядом с каждым курсом конкурента показывается, **когда его переставил сам банк**
  (не когда мы собрали). Старше 3 часов — оранжевым.

## Отделения УРКО (сейчас скрыты с сайта)

Раз в неделю Игорь дежурит, и на это время ему добавляют ещё 81 отделение (41 в Москве,
40 в области). Они лежат отдельно и включаются слоем «Отделения УРКО» на карте
(и галкой на вкладке «Таблица»); выбор запоминается в браузере. Маркеры светлые
с пунктирной рамкой, в карточке — метка «УРКО» и город.

⚠️ **Сейчас слой скрыт по просьбе Игоря.** Данные, сборщики и таймеры продолжают работать —
чтобы вернуть на сайт, достаточно поставить `const SHOW_DUTY = true` в `index.html`
и в `table.html` (по одной строке в каждом), закоммитить и запушить.

- Состав: `scripts/branch-ids-duty.json`, данные — `data/branches_duty.js` (`build_data.py --fetch`).
- Конкуренты вокруг московских дежурных берутся из того же московского фида — бесплатно.
- Для области отдельный сборщик `collect_oblast.py`: страницы городов на mainfin,
  таймер `unistream-oblast.timer` раз в 2 часа. ⚠️ Города без своей страницы редиректят
  на Москву — такие пропускаем (иначе в карточку уедут московские курсы). Сейчас покрыто
  15 городов из 25: нет Мытищ, Люберец, Пушкино, Видного, Дедовска, Котельников и др.
- Курсы дежурных отделений подтягиваются только когда слой включён (81 запрос, пул по 8).

## Вкладки сайта

- `index.html` — карта: поиск станции метро, ближайшие отделения, конкуренты на карте.
- `table.html` — табличный вид как в Excel: строка на отделение, разворачивается в список
  соседей с курсами. Переключатель валюты, фильтр «только где у соседей выгоднее»,
  выгрузка CSV и печать. Красным — курс выгоднее нашего для клиента, зелёным — наш не хуже.

## Excel-отчёты

```bash
python3 scripts/build_report.py            # внутренний: report/Конкуренты_ЮНИСТРИМ_ГГГГ-ММ-ДД.xlsx
python3 scripts/build_report.py --clean    # для руководства: report/Курсы_рядом_с_отделениями_ГГГГ-ММ-ДД.xlsx
```

- **Внутренний** — с оценками: сколько соседей выгоднее нас, где открыто окно перепродажи,
  выделения красным. Для работы, не для отправки наверх.
- **Для руководства** (`--clean`) — только факты: какие банки рядом, адреса, расстояния,
  их курсы и время обновления, диапазон курсов по округе. Без сравнений, выводов и подсветки.

В обоих случаях лист «Сводка» + лист на каждое отделение; наши курсы тянутся живыми
из API ЮНИСТРИМ в момент сборки. Папка `report/` в `.gitignore` — файлы разовые.

## Окна перепродажи и частота изменений

Если сосед **продаёт** валюту дешевле, чем мы её **покупаем**, в карточке отделения появляется
красное предупреждение: валюту могут купить рядом и сдать нам — на разнице теряем мы.
Считается по живым курсам Юнистрима и последним курсам конкурентов.

Частота изменений курса копится в `scripts/state/rate_history.json` (вне git) и публикуется
в `data/volatility.js`. Метрика — **доля наших замеров, в которых курс оказался новым**:
замеры идут раз в 2 часа, поэтому «сколько раз в день» честно измерить нельзя, а вот
«успевает ли курс устареть между заходами» — можно. Подпись в интерфейсе появляется только
когда накоплено ≥1 суток и ≥8 сравнений, иначе молчит.

```bash
python3 scripts/rate_history.py --stats      # кто как часто переставляет курс
python3 scripts/rate_history.py --backfill   # добрать точки из git-истории competitors.js
python3 scripts/check_rate_spread.py [USD]   # одинаковый ли курс во всех отделениях банка
```

```bash
python3 scripts/collect_competitors.py --dry-run   # курсы офисов (banki.ru)
python3 scripts/collect_bank_rates.py --dry-run    # курсы банков по городу (mainfin) + что не совпало
systemctl start unistream-competitors.service      # разовый прогон обоих
journalctl -u unistream-competitors.service -n 30  # что было в последний раз
```

Банки, по которым курса нет ни в одном источнике (МКБ, Росбанк, ОТП, Кредит Европа, Дом.РФ, БКС и
длинный хвост мелких), остаются в слое «Другие банки рядом» без курса. Их сайты — JS-приложения
за антиботом (МКБ отдаёт 403), сертификаты у части банков от Минцифры: для запросов с сервера нужен
их корневой CA (`gu-st.ru/content/lending/russian_trusted_root_ca_pem.crt`), иначе SSL-ошибка.
Почта Банк присоединён к ВТБ и своих курсов больше не публикует.

Точки без публикуемых курсов (слой «Другие банки рядом») — из OpenStreetMap, меняются редко:

```bash
python3 scripts/collect_osm_poi.py    # раз в месяц-два
```
